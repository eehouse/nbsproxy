* Use names, not appIDs, in everything user-visible.

* Would be useful in testing NBS apps to have the send-to-self case
  avoid the phone network for super-fast speeds that'd stress-test
  service code.
